{
  "private": true,
  "dependencies": {
    "@heroicons/react": "^1.0.3",
    "gray-matter": "^4.0.3",
    "next": "^11.0.1",
    "react": "^17.0.2",
    "react-dom": "^17.0.2",
    "react-markdown": "^6.0.2"
  },
  "scripts": {
    "dev": "next dev",
    "prettier": "prettier --write ./pages ./lib ./components ./hooks ./contexts",
    "build": "yarn run prettier && next build",
    "export": "yarn run build && next export -o build"
  },
  "license": "MIT",
  "devDependencies": {
    "autoprefixer": "^10.3.1",
    "classnames": "^2.3.1",
    "postcss": "^8.3.6",
    "prettier": "^2.3.2",
    "tailwindcss": "^2.2.7"
  }
}